public int loneSum(int a, int b, int c) {
  int sum = 0;
  
    sum = sum + a + b + c;
  if(b == c){
    sum = a;
  }
  if(c == a){
    sum = b;
  }
  if(a == b){
    sum = c;
  }
  if(a == b && b == c && c == a){
    sum = 0;
  }
  
  return sum;
}
